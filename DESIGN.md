# PLKIT 데스크톱 대시보드 디자인 시스템

## 0. 리서치 기록

- 내장 레퍼런스: 밀도 높은 운영형 제품을 위해 Linear, Vercel, Raycast를 검토했습니다. 절제된 정보 위계, 간결한 상태 언어, 데스크톱 작업 흐름에 적합한 리듬을 가진 Linear를 참고 방향으로 선택했습니다. 상표 자산이나 문구는 사용하지 않습니다.
- Lazyweb 제품 화면 리서치: 이번 티켓은 제품 리디자인이 아니라 시작 상태만 보여 주는 단일 화면으로 범위를 제한하므로 수행하지 않았습니다. 완료 조건에 외부 시각 레퍼런스는 필요하지 않습니다.
- Imagen 콘셉트: 복잡한 Dashboard 디자인이 필요 없는 초기 설정 티켓이므로 생성하지 않았습니다. 생성된 목업은 불필요한 픽셀 단위 구현 계약이 될 수 있습니다.

## 1. 제품 의도

PLKIT는 스마트팜용 데스크톱 대시보드입니다. 초기 화면은 로컬 데스크톱 애플리케이션이 실행되었음을 알릴 뿐, 센서·장치·카메라·API·AI 서비스가 연결되었다는 의미는 아닙니다.

차분한 운영 화면을 지향합니다. 따뜻한 오프화이트 배경, 식물 녹색의 준비 상태, 하나의 입체적인 흰색 패널을 사용합니다. 작고 녹색인 준비 상태 표시가 제품의 현재 의미를 과장하지 않고 전달하는 핵심 요소입니다.

## 2. 토큰

| 범주   | 토큰                     | 값                                | 용도                      |
| ------ | ------------------------ | --------------------------------- | ------------------------- |
| 캔버스 | `--color-canvas`         | `#f6f8f5`                         | 창 배경                   |
| 표면   | `--color-surface`        | `#ffffff`                         | 기본 패널                 |
| 표면   | `--color-surface-muted`  | `#edf2ec`                         | 예약된 보조 표면          |
| 텍스트 | `--color-text-primary`   | `#10251e`                         | 제목과 주요 문구          |
| 텍스트 | `--color-text-secondary` | `#53635c`                         | 보조 문구                 |
| 경계선 | `--color-border`         | `#dce5de`                         | 향후 구분선과 입력 요소   |
| 강조   | `--color-accent`         | `#16794a`                         | 준비 상태 표시            |
| 강조   | `--color-accent-soft`    | `#dff2e7`                         | 준비 상태 배경            |
| 입체감 | `--shadow-panel`         | `0 18px 45px rgb(20 53 37 / 10%)` | 기본 패널 깊이            |
| 형태   | `--radius-panel`         | `20px`                            | 기본 패널 모서리          |
| 여백   | `--space-1`~`--space-6`  | `4px`~`32px`                      | 레이아웃 리듬             |
| 글자   | `--font-size-label`      | `0.75rem`                         | 작은 레이블과 상태 텍스트 |
| 글자   | `--font-size-body`       | `1rem`                            | 보조 문구                 |
| 글자   | `--font-size-title`      | `clamp(2.25rem, 6vw, 4rem)`       | 주 제목                   |

글꼴은 운영체제 UI 글꼴과 한글 대체 글꼴인 `Segoe UI Variable`, `Noto Sans KR`, `Malgun Gothic`을 순서대로 사용합니다. 원격 글꼴 요청 없이도 Electron 창에서 읽기 쉬운 한글을 표시하기 위함입니다.

## 3. 레이아웃과 반응형 규칙

- 애플리케이션 셸은 최소 `100dvh`를 채우며 하나의 패널을 중앙에 배치합니다.
- 패널은 최대 너비 `680px`, 모서리 `20px`, 내부 여백 `32px`~`64px`를 사용합니다.
- 보조 문구는 최대 줄 길이 `40rem`과 `word-break: keep-all`을 적용해 한글 가독성을 유지합니다.
- 초기 경로는 Dashboard 하나뿐이며 스크롤, 메뉴, 제어 기능, 네트워크 상태를 제공하지 않습니다.

## 4. 모션

장식 목적의 애니메이션은 사용하지 않습니다. 초기 화면에는 모션 피드백이 필요한 사용자 상호작용이 없습니다.

## 5. 재사용 프리미티브와 상태

- `DashboardPanel`: 초기 애플리케이션 페이지를 위한 단일 입체 표면입니다. 상태는 기본 상태 하나입니다.
- `ReadinessStatus`: 녹색 상태 점과 텍스트입니다. 상태는 애플리케이션 준비 완료만 의미하며 센서·네트워크·API·장치의 가용성을 나타내면 안 됩니다.

## 6. 접근성 제약

- 페이지는 하나의 보이는 `h1`과 의미론적인 `main`, `section` 랜드마크를 사용합니다.
- 준비 상태 메시지는 `role="status"`를 사용합니다. 상태 점은 텍스트가 의미를 전달하므로 보조 기술에서 숨깁니다.
- 한글 보조 문구는 조사 분리나 글리프 잘림을 피할 수 있는 너비를 유지합니다.
- 색상만으로 준비 상태를 전달하지 않으며 텍스트로 상태를 명시합니다.

## 7. 허용된 기술 부채

- 이번 티켓에는 실제 프리미티브 하나와 최소 Dashboard 페이지 하나만 있으므로 전체 프리미티브 쇼케이스는 미뤘습니다. 패널·제어·카드·내비게이션을 추가할 때는 사용 전 이 문서에 정의합니다.
- React 개발 진단 도구는 설치하지 않았습니다. 현재 초기 구성은 요청된 Electron/React/Vite와 품질 도구만 유지합니다. 실제 renderer 컴포넌트 개발을 시작할 때 도입합니다.

## 8. 인수인계

기능이나 페이지 CSS에는 하드코딩한 시각 값을 넣지 말고 `src/renderer/app/styles/global.css`의 CSS 사용자 속성을 사용합니다. 도메인 특화 UI는 `shared/ui`가 아니라 소유 FSD 슬라이스에 두고 해당 슬라이스의 `index.ts`로 공개합니다.
